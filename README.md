
  # External Room configuration

  This is a code bundle for External Room configuration. The original project is available at https://www.figma.com/design/aAyhBlef5r0j2HnoXmUhKf/External-Room-configuration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  